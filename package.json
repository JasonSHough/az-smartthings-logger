{
  "name": "backend",
  "version": "1.0.4",
  "description": "",
  "scripts": {
    "terraform:build": "copyfiles -f src/terraform/*.tf config/\"${ENV:=dev}\"/settings.tf build/terraform && echo appversion='\"'$npm_package_version'\"\n'funcqcsv='\"'../funcqcsv-$npm_package_version.zip'\"' > build/terraform/terraform.tfvars",
    "terraform:apply": "terraform -chdir=build/terraform init && terraform -chdir=build/terraform validate && terraform -chdir=build/terraform apply",
    "clean": "rimraf build",
    "prebuild:zip": "mkdirp --mode=0700 build",
    "build:zip": "jszip-cli --output build/funcqcsv-$npm_package_version.zip add src/funcqcsv/*",
    "build": "run-s clean build:zip terraform:build",
    "predeploy": "npm run build",
    "deploy": "run-s terraform:apply"
  },
  "dependencies": {
    "dotenv": "^8.2.0"
  },
  "devDependencies": {
    "@azure/functions": "^1.2.2",
    "@ffflorian/jszip-cli": "^3.1.0",
    "azure-functions-core-tools": "^2.7.3188",
    "copyfiles": "^2.4.1",
    "dotenv-cli": "^4.0.0",
    "mkdirp": "^1.0.4",
    "npm-run-all": "^4.1.5",
    "rimraf": "^3.0.2"
  }
}
